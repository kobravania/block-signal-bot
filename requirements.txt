# Telegram bot
aiogram==3.*

# Web framework
fastapi==0.115.*

# ASGI server
uvicorn[standard]==0.35.*

# HTTP client
httpx==0.27.*

# Env config (чтобы хранить токены в .env)
python-dotenv==1.*

# Планировщик задач (например, напоминания/cron для сигналов)
apscheduler==3.*

# Coinbase Commerce SDK (работа с API и webhooks)
coinbase-commerce==1.*

# Для работы с ассинхронными задачами (если нужно в будущем)
redis==5.*

# Дополнительно (удобный парсер JSON / дата-время)
pydantic==2.*
